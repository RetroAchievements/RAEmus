App.cpp is encoded in "Japanese (MAC)", which I can't get to load in the VS2017 editor - some 
of the characters contain embedded quotes which break the editor parser.

The file does compile through the IDE just fine, but I found that I had to use an external
editor to modify it.